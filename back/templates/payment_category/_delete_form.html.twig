<form method="post" action="{{ path('payment_category_delete', {'idpaymentCategory': payment_category.idpaymentCategory}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment_category.idpaymentCategory) }}">
    <button class="btn">Delete</button>
</form>
