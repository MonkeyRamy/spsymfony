{% extends 'base.html.twig' %}

{% block title %}PaymentCategory{% endblock %}

{% block body %}
    <h1>PaymentCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdpaymentCategory</th>
                <td>{{ payment_category.idpaymentCategory }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ payment_category.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('payment_category_index') }}">back to list</a>

    <a href="{{ path('payment_category_edit', {'idpaymentCategory': payment_category.idpaymentCategory}) }}">edit</a>

    {{ include('payment_category/_delete_form.html.twig') }}
{% endblock %}
