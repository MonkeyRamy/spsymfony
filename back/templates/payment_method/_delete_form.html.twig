<form method="post" action="{{ path('payment_method_delete', {'idpayingMethod': payment_method.idpayingMethod}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment_method.idpayingMethod) }}">
    <button class="btn">Delete</button>
</form>
