{% extends 'base.html.twig' %}

{% block title %}CategoryType{% endblock %}

{% block body %}
    <h1>CategoryType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdcategoryType</th>
                <td>{{ category_type.idcategoryType }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ category_type.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_type_index') }}">back to list</a>

    <a href="{{ path('category_type_edit', {'idcategoryType': category_type.idcategoryType}) }}">edit</a>

    {{ include('category_type/_delete_form.html.twig') }}
{% endblock %}
